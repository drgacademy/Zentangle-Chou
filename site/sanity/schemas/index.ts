import { artwork } from './artwork';
import { tangle } from './tangle';
import { video } from './video';
import { homepage, aboutPage, siteSettings } from './homepage';

export const schemaTypes = [artwork, tangle, video, homepage, aboutPage, siteSettings];
